# Roster Copilot Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control

# =============================================================================
# AI Services
# =============================================================================

# Google Gemini AI API Key
# Used by the AI Copilot Service for generating roster recommendations
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# Email Services
# =============================================================================

# Resend API Key
# Used by the Notification Service for sending transactional emails
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Default "from" email address for notifications
# Must be a verified domain in Resend (or use "noreply@resend.dev" for testing)
DEFAULT_FROM_EMAIL="noreply@yourdomain.com"

# =============================================================================
# Authentication
# =============================================================================

# JWT Secret for token signing and verification
# Use a strong, random string (at least 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_secure_jwt_secret_here_at_least_32_characters_long

# JWT Token Expiration (in seconds)
# Default: 3600 (1 hour) - Optional, will use default if not set
# JWT_EXPIRES_IN=3600

# =============================================================================
# Application Configuration
# =============================================================================

# Application Base URL
# Used for generating email verification links and redirects
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Base URL for API calls
# Usually same as NEXT_PUBLIC_APP_URL for Next.js apps
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# Development Configuration (Optional)
# =============================================================================

# Port for development server
# Default: 3000
# PORT=3000

# Node Environment
# Values: development, production, test
# NODE_ENV=development

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite database path (relative to project root)
# Default: data/roster_copilot_poc.db
# Note: This is handled automatically by the application
# DB_PATH=data/roster_copilot_poc.db

# =============================================================================
# Security Notes
# =============================================================================

# 1. Never commit this file with real values to version control
# 2. Use strong, unique values for all secrets
# 3. Rotate API keys and secrets regularly
# 4. Use different values for development, staging, and production
# 5. Store production secrets securely in your deployment platform

# =============================================================================
# Getting Started
# =============================================================================

# 1. Copy this file to .env.local (or rename existing .env to .env.local)
# 2. Replace all placeholder values with your actual credentials
# 3. Ensure .env.local is in your .gitignore file
# 4. Run: npm run dev:seeded to start with sample data

# =============================================================================
# Required Variables Summary
# =============================================================================

# These are the minimum required variables for the application to work:
# - GEMINI_API_KEY (for AI features)
# - RESEND_API_KEY (for email notifications)
# - JWT_SECRET (for authentication)
# - NEXT_PUBLIC_BASE_URL (for API calls and redirects)
# - DEFAULT_FROM_EMAIL (for email sender address)